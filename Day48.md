### Day48 单调栈02

## 42. Trapping Rain Water(接雨水)
> **Tags**: [`单调栈`](/search?q=单调栈)
本题可以采用双指针法，核心逻辑在于利用木桶效应通过左右双指针向中间逼近来动态维护边界高度。由于每一列的接水高度由其左右两侧最高柱子中的较小值决定，我们使用 left_max 和 right_max 记录指针移动过程中遇到的左侧和右侧最大高度。遍历顺序为左右指针相向而行，在每一轮迭代中，通过比较 height[left] 与 height[right] 的大小来决定处理哪一侧：始终优先处理高度较小的一侧，因为此时可以确定该侧的“短板”即为对应的 left_max 或 right_max，而另一侧必然存在一个足够高的挡板支撑水位。具体操作时，若左侧较低，则判断当前高度是否超过 left_max，若未超过则计算差值累加至结果，随后移动指针。这种方式规避了暴力解法中对每个位置重复扫描左右最高点的冗余，将空间复杂度降至 O(1)，在一次遍历中通过实时更新的局部最优解达成了全局的水量统计。

同样的思路还可以使用单调栈解决，其核心是通过维护一个单调递减栈来寻找并横向计算每一个“凹槽”的蓄水量。当遍历到的柱子高度大于栈顶时，意味着找到了一个右边界，此时弹出栈顶作为凹槽底部，并利用新的栈顶作为左边界，通过 min(left, right) - bottom 计算出当前层的水位高度，再乘以左右边界间的下标差计算宽度。这种方法将复杂的凹凸结构分解为多个横向的长方形区域，在 O(n) 时间内利用栈的后进先出特性，确保每个位置都能与其左侧第一个比它高的元素匹配，从而精准捕捉所有集水坑洞。

## 84. Largest Rectangle in Histogram(84.柱状图中最大的矩形)
> **Tags**: [`单调栈`](/search?q=单调栈)
本题利用单调栈寻找每个柱子左右两侧第一个比其矮的边界，从而确定以当前柱子为高的最大矩形面积。核心逻辑在于维护一个单调递增栈，当遇到比栈顶更矮的元素 $i$ 时，说明栈顶元素 $mid$ 的右边界已确定，而栈中 $mid$ 下方的元素 $stack[-1]$ 即为其左边界。此时弹出的 $heights[mid]$ 即为矩形高度，宽度则由左右边界下标差 $(i - stack[-1] - 1)$ 确定。通过在原数组首尾添加哨兵元素 $0$，可以确保栈内所有元素都能在遍历结束前完成触发结算，并简化了空栈判断。这种方法将原本需要 $O(n^2)$ 的区间搜索优化至 $O(n)$，通过一次遍历即可横向计算出所有可能的极大矩形面积并取其最大值。
